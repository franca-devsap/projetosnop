sap.ui.define([
  "sap/ui/core/UIComponent"
], function (UIComponent) {
  "use strict";

  return UIComponent.extend("zsd.forecast.unrestricted.Component", {
    metadata: {
      manifest: "json"
    }
  });
});
